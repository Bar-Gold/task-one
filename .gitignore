# Node modules, should anyone run npm here. The validation tooling itself
# lives on the `tooling` branch; this branch is the site and nothing else.
node_modules/

# Build artefacts
dist/
*.zip

# Logs
*.log
npm-debug.log*

# OS cruft
.DS_Store
Thumbs.db
desktop.ini
