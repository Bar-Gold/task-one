{
  "name": "task-one-validation",
  "version": "1.0.0",
  "private": true,
  "description": "Validation tooling for the digital business card. Build-time only - the site itself ships zero JavaScript.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint:html": "html-validate ../index.html",
    "lint:css": "stylelint \"../css/*.css\""
  },
  "devDependencies": {
    "html-validate": "^9.5.3",
    "node-html-parser": "^6.1.13",
    "stylelint": "^16.10.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
